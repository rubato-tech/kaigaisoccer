# 海外サッカー日程サイト 無料公開ガイド

このプロジェクトは Manus の **ビルトイン無料ホスティング** にそのまま公開できます。Vercel・Netlify・Railway 等の外部サービスに移す必要はなく、ボタンひとつでデプロイ可能です。

> 公開するとサイトは自動的に `https://<好きな名前>.manus.space` というアドレスで世界に公開されます。Manus 内蔵 DB・サーバー・スケジュールタスクが追加課金なしで利用できます。

---

## 1. 公開手順（3ステップ）

### ステップ① 画面右上の「公開（Publish）」ボタンを押す

- チャット画面の右側にある **Management UI** ヘッダーの右上に **Publish** ボタンがあります。
- このボタンが押せる状態になっているのは、直前にチェックポイントが保存されているためです（既に `4b68b718` を保存済）。
- クリックするとビルドが走り、数十秒〜数分でデプロイ完了します。

### ステップ② サブドメインを決める（任意）

- 初回公開時は `xxxxxxxx.manus.space` のランダムな文字列が割り当てられます。
- そのままでも問題ありませんが、覚えやすい名前に変更したい場合は **Settings → Domains** で好きなサブドメインへ変更できます（例：`kaigai-soccer.manus.space`）。
- 独自ドメイン（`example.com` など）を持っている場合も、同じ Domains 画面から「カスタムドメインを接続」できます。Manus 内でドメインの新規購入も可能です。

### ステップ③ 公開後に「自動更新スケジュール」を1回だけ登録する

公開後にこのチャットで「**公開しました**」または「**スケジュール登録お願い**」と言ってください。私が以下の自動同期タスクを設定します：

- **毎日 朝5時（JST）に試合データを TheSportsDB から再取得して DB を更新**
- 更新時間は変更可能（例：3時、6時、12時間ごと等）
- 公開済みのサイトに対してのみ動作する仕様のため、必ず公開後に登録してください。

---

## 2. 公開後にできること

| メニュー | 場所 | できること |
|---|---|---|
| **Preview** | Management UI 左上 | 公開サイトと同じUIで動作確認できる |
| **Code** | Management UI | 全ソースコードのダウンロード（ZIP形式） |
| **Database** | Management UI | DBを直接閲覧・編集できる管理画面 |
| **Settings → General** | Management UI | サイト名・ファビコンの変更 |
| **Settings → Domains** | Management UI | サブドメイン変更／独自ドメインの接続・購入 |
| **Settings → GitHub** | Management UI | コードを自分の GitHub リポジトリにエクスポート |
| **Dashboard** | Management UI | 公開後のアクセス数（PV/UV）を計測 |

---

## 3. 料金について

- **Manus 内蔵ホスティング**: 通常利用は無料枠の範囲で運用可能です。
- 試合データ取得元の **TheSportsDB** も無料 API（free key）を使用しているため追加コストなし。
- アクセス数が大幅に増えた場合のみ、Manus のアップグレードが必要になることがあります（不要なら気にしなくてOK）。

---

## 4. よくある質問

### Q. 公開後にデザインや機能を直したい場合は？

このチャットで「○○を直して」と指示するだけで OK です。私が修正→チェックポイント保存まで行います。その後、再度 Publish ボタンを押せば反映されます。

### Q. 独自ドメイン（example.com）で公開できる？

可能です。**Settings → Domains** で「Add custom domain」から接続できます。新規購入も同じ画面でできます。

### Q. データの更新が止まったらどうする？

公開後に登録するスケジュールタスクが毎日自動で実行されます。万が一止まったら、サイトの「再読み込み」ボタンを押すか、管理画面から `/api/scheduled/refresh` を直接叩くことで手動更新できます。

### Q. ソースコードを自分の手元に持ちたい

**Settings → GitHub** から自分の GitHub アカウントの新規リポジトリにエクスポートできます。または **More メニュー（⋯）→ Download as ZIP** でダウンロード可能です。

---

## 5. このサイトの主な仕様（公開後の挙動）

| 項目 | 内容 |
|---|---|
| 対応リーグ数 | 欧州12 + UEFA 3 + 代表戦 = 約 4,000 試合 |
| 表示時刻 | 日本時間（JST）、深夜キックオフは 25:00〜27:59 表記 |
| タブ | 欧州日程／欧州結果／日本人選手出場試合／代表戦日程 |
| フィルタ | リーグ複数選択 + チーム名検索（原語・カタカナ両対応） |
| 自動更新 | 毎日 5:00 JST（公開後にスケジュール登録） |
| データ元 | TheSportsDB（free key, 商用利用可） |

---

何か不明点があれば、このチャットで気軽に質問してください。
